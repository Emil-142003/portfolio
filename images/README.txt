Place your profile image here as `profile.jpg` (recommended 400x400px, JPG/PNG).

You can save the photo you uploaded via the chat as `images/profile.jpg` so the portfolio displays it automatically.

If you want, tell me and I will save the uploaded image into this path for you.